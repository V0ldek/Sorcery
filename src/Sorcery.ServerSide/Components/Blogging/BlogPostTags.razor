@using Sorcery.Blogging;

@foreach (var tag in Tags.Take(Tags.Count - 1))
{
    <span><Sorcery.ServerSide.Components.Link Typo="@Typo.inherit" Href="@($"/sourcery/tags/{tag}")">@tag</Sorcery.ServerSide.Components.Link>, </span>
}

@if (Tags.Any())
{
    <Sorcery.ServerSide.Components.Link Typo="@Typo.inherit" Href="@($"/sourcery/tags/{Tags[^1]}")">@Tags[^1]</Sorcery.ServerSide.Components.Link>
}

@code {
    [Parameter]
    [EditorRequired]
    public IReadOnlyList<Tag> Tags { get; set; } = Array.Empty<Tag>();
}
