@using Sorcery.Blogging;

@foreach (var tag in Tags.Take(Tags.Count - 1))
{
    <span><LinkA Typo="@Typo.inherit" Href="@($"/sourcery/tags/{tag}")">@tag</LinkA>, </span>
}

@if (Tags.Any())
{
    <LinkA Typo="@Typo.inherit" Href="@($"/sourcery/tags/{Tags[^1]}")">@Tags[^1]</LinkA>
}

@code {
    [Parameter]
    [EditorRequired]
    public IReadOnlyList<Tag> Tags { get; set; } = Array.Empty<Tag>();
}
